spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&autoReconnect=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  devtools:
    restart:
      enabled: false
  thymeleaf:
    cache: false
    mode: LEGACYHTML5
mybatis:
  typeAliasesPackage: com.nightriver.jungle.common.pojo
  mapper-locations: classpath:mapping/*.xml
# 打印SQL语句
logging:
  level:
    com:
      pqtel:
        pqcloud:
          core:
            dao: debug