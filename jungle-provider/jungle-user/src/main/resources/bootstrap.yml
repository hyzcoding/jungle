server:
  port: 9001
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: jungle-user
  cloud:
    config:
      profile: dev
      uri: http://localhost:7000/
      discovery:
        service-id: jungle-config
        enabled: true
